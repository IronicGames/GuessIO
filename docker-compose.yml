services:
  postgres:
    image: postgres:16-alpine
    container_name: guess-io-db
    restart: always
    ports:
      - '${POSTGRES_PORT}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env

volumes:
  postgres_data:
